# BindIQ Agent 1 — Data Extractor Dependencies

# HTTP + HTML parsing
requests>=2.31.0
beautifulsoup4>=4.12.0
# lxml: setup.bat installs this first with --only-binary to avoid C compilation on Windows
lxml>=5.1.0

# For MoneyGeek JS-heavy pages (use if static HTML parsing fails)
selenium>=4.18.0
webdriver-manager>=4.0.0

# LLM extraction (Claude Haiku for RSC/JS-heavy pages)
anthropic>=0.40.0

# Data handling
python-dotenv>=1.0.0
