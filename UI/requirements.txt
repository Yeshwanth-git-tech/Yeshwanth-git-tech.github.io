# BindIQ UI — FastAPI Backend + Streamlit Dashboard

# FastAPI (primary API server — ngrok / ServiceNow integration)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Streamlit (optional local UI — run with: streamlit run app.py)
streamlit>=1.32.0

python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.40.0
numpy>=1.24.0

# Neo4j (shared with KnowledgeGraph)
neo4j>=5.14.0

# Graph visualization
pyvis>=0.3.2

# Email monitoring
# Note: uses built-in imaplib + smtplib (no extra package for email)
flask>=3.0.0

# Sentence transformers (email agent Stage 1 embedding)
sentence-transformers>=2.7.0
